{% set name = "fastavro" %}
{% set version = "0.21.10" %}
{% set sha256 = "691b7f7217fdd4dad7fb3a8ad1384e30b9ed60a04d050fdf3ebd440c9f9601a2" %}

package:
    name: {{ name }}
    version: {{ version }}

source:
    fn: {{name}}-{{ version }}.tar.gz
    url: https://files.pythonhosted.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
    sha256: {{ sha256 }}

build:
    number: 1000
    script: python setup.py install --single-version-externally-managed --record record.txt


requirements:
    build:
        - {{ compiler('c') }}
    host:
        - python
        - cython
        - setuptools
    run:
        - python

test:
    requires:
        - nose
        - flake8
    imports:
        - {{ name }}

about:
    home: https://github.com/fastavro/{{ name }}
    license: MIT
    summary: Fast read/write of AVRO files
    dev_url: https://github.com/fastavro/{{ name }}

extra:
    recipe-maintainers:
        - cpcloud
        - mariusvniekerk
        - scottbelden
        - tebeka
