{% set name = "fastavro" %}
{% set version = "0.21.8" %}
{% set sha256 = "06b7c4ba99f4194725e7832fb6641ef24d6b428dcb8c1f1fb452525df3b45ef4" %}

package:
    name: {{ name }}
    version: {{ version }}

source:
    fn: {{name}}-{{ version }}.tar.gz
    url: https://files.pythonhosted.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
    sha256: {{ sha256 }}

build:
    number: 1000
    script: python setup.py install --single-version-externally-managed --record record.txt


requirements:
    build:
        - {{ compiler('c') }}
    host:
        - python
        - cython
        - setuptools
    run:
        - python

test:
    requires:
        - nose
        - flake8
    imports:
        - {{ name }}

about:
    home: https://github.com/fastavro/{{ name }}
    license: MIT
    summary: Fast read/write of AVRO files
    dev_url: https://github.com/fastavro/{{ name }}

extra:
    recipe-maintainers:
        - cpcloud
        - mariusvniekerk
        - scottbelden
        - tebeka
